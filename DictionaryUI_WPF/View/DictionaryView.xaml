<UserControl x:Class="DictionaryUI_WPF.View.DictionaryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DictionaryUI_WPF.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid >
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>

    <ListBox x:Name="themesListBox" Grid.Column="0"
             SelectionChanged="ThemesListBox_SelectionChanged"
             Background="White">
        
        <!-- Добавление тем -->
        
    </ListBox>

    <ListView x:Name="wordsListView" Grid.Column="1" Background="White">
        <ListView.View>
            <GridView>
                <GridViewColumn Header="Слова" Width="150"/>
            </GridView>
        </ListView.View>
    </ListView>
</Grid>
        
        
    </Grid>
</UserControl>
